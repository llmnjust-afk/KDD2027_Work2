\section{Experiments and Results}
\label{sec:exp}

\subsection{Experimental Setup}
\label{sec:setup}

\textbf{Models.} We evaluate five frontier LLMs via an OpenAI-compatible endpoint: GPT-4o, GPT-4o-mini, DeepSeek-V3 (\texttt{deepseek-chat}), DeepSeek-R1 (\texttt{deepseek-reasoner}), and Qwen3-max (\texttt{qwen3-max-2026-01-23}). All models use temperature 0 and a maximum of 6--8 ReAct steps.

\textbf{Task suites.} We use three complementary task suites (\Cref{tab:task-summary}):

\input{tables/table_task_summary}

\textbf{Protocol.} Each (model, task) pair is run for 3 repetitions. The main experiment (95 tasks $\times$ 5 models $\times$ 2 variants $\times$ 3 repeats) yields 2{,}850 runs on synthetic tasks; the UCI experiment (23 tasks $\times$ 5 models $\times$ 3 repeats) yields 345 runs; the DSBench experiment (200 tasks $\times$ 3 models $\times$ 3 repeats + 164 Qwen3-max runs) yields 1{,}964 runs. In total we analyse \textbf{5{,}984 agent runs} and \textbf{3{,}940 failures}. All tables in this paper are auto-generated from a single source-of-truth manifest with assertion checks\footnote{See \texttt{build\_manifest\_and\_tables.py} in our supplementary code.}.

\textbf{Statistical tests.} Verifier comparisons use the McNemar test (appropriate for paired binary outcomes), reporting $b$ (baseline correct \& verifier wrong), $c$ (baseline wrong \& verifier correct), and the $\chi^2$ $p$-value.

\subsection{Result 1: Comprehensive Model Comparison}
\label{sec:res-models}

\input{tables/table_comprehensive}

\Cref{tab:comprehensive} presents the comprehensive baseline results across all five models, three task suites, and nine diagnostic metrics. Three patterns emerge. First, \textbf{DeepSeek-V3 is the strongest model} on both synthetic (88.4\% SR) and UCI (94.2\% SR) tasks, achieving perfect code-execution success on synthetic traps. Second, \textbf{the silent-failure rate is strongly task-dependent}: on synthetic trap tasks, SFR ranges from 39\% to 100\% (failures are often wrong answers, not crashes); on UCI real data, SFR is bimodal---either 0\% (GPT-4o-mini, DeepSeek-V3) or 100\% (GPT-4o); on DSBench, only GPT-4o produces silent failures (38.3\%). Third, \textbf{DSBench is extremely challenging} for all models (0--3.3\% SR), with GPT-4o achieving the highest success rate and the only non-zero SFR, indicating that even the strongest model produces plausible-but-wrong answers on complex financial data.

\input{tables/table_main_results}

The corrected SFR reveals a task-dependent pattern: on synthetic trap tasks, SFR is 39--100\%; on UCI real data, SFR ranges from 0\% to 100\% depending on model; on DSBench, SFR is 0--38\%. The high SFR is specific to tasks where code runs but reasoning fails, not a universal property.

\subsection{Result 2: Failure Bifurcation}
\label{sec:res-bifurcation}

\input{tables/table_null_model}

\Cref{tab:null-model} reports SFR by task suite. The SFR is 86.2\% on synthetic tasks, 3.8\% on UCI, and 18.9\% on DSBench. We note that SFR and execution-success rate share the variable $E$ (loud failures), so their correlation is partially mechanical. The $R^2 = 0.493$ is reported as a \emph{descriptive statistic}, not a causal decomposition: it cannot be interpreted as ``49\% mechanical, 51\% real.'' A proper test would require a multinomial mixed-effects model with model, task difficulty, and suite as random effects, which we leave to future work.

\begin{figure}[t]
\centering
\includegraphics[width=0.9\linewidth]{figures/bifurcation.pdf}
\caption{SFR vs.\ success rate across task suites and models.}
\label{fig:bifurcation}
\end{figure}

\subsection{Result 3: Taxonomy Validation}
\label{sec:res-taxonomy}

We validate the five-stage taxonomy via LLM-as-judge annotation (GPT-4o-mini, 100 traces, blind to the rule-based label). After fixing a classifier bug that incorrectly labeled \texttt{final\_answer=None} traces as silent, Cohen's $\kappa = 0.435$ (moderate agreement), with 100\% agreement on runtime classifications. The remaining disagreement centers on the boundary between \texttt{output\_mismatch} and \texttt{answer\_error}. We acknowledge that LLM-as-judge cannot replace human annotation; full human validation with $\geq 3$ annotators is needed for a definitive $\kappa$.

\subsection{Result 4: Zero Self-Correction}
\label{sec:res-recovery}

Across all 5{,}984 runs and all five models, the recovery rate is 0\%: no agent ever corrected a silent failure without external intervention. We note that this is partly a consequence of the experimental protocol: silent failures produce no error signal, so the agent has no opportunity to detect and retry. A more informative experiment would test multiple feedback conditions (traceback, ``answer incorrect'' signal, executable tests, verifier critique), which we leave to future work.

This zero-recovery finding has a strong practical implication: in a multi-step pipeline, a silent failure at step $k$ propagates to all downstream steps, and the agent will never detect or fix it. External verification is therefore mandatory for any deployment that cannot tolerate wrong answers.

\subsection{Result 5: Propagation Depth Analysis}
\label{sec:res-propagation}

\Cref{fig:propdepth} (in \Cref{sec:propagation}) shows the overall propagation depth distribution. Here we analyse propagation depth \emph{by model}, revealing a striking pattern (\Cref{fig:propdepth-model}). GPT-4o has a mean propagation depth of 1.10 steps with 24.8\% of its failures reaching long-horizon status (depth $\geq$ 2)---far higher than any other model. GPT-4o-mini has a mean depth of 0.25 (7.1\% long-horizon), while DeepSeek-V3, DeepSeek-R1, and Qwen3-max all have zero propagation depth, meaning their failures are detected immediately.

\begin{figure}[t]
\centering
\includegraphics[width=0.95\linewidth]{figures/propdepth_by_model.pdf}
\caption{Propagation depth by model (baseline, all suites). GPT-4o exhibits the longest failure propagation (mean 1.10, 24.8\% long-horizon), while other models detect failures immediately. This reflects GPT-4o's tendency to continue reasoning on a wrong intermediate result rather than crashing.}
\label{fig:propdepth-model}
\end{figure}

This model-dependent propagation is diagnostically meaningful: GPT-4o's higher SR (62.1\% on synthetic) means it rarely crashes, but when it does fail silently, it tends to \emph{build on} the wrong intermediate result across multiple steps, wasting tokens and making the failure harder to localise. In contrast, DeepSeek-V3's failures are concentrated in the final answer (depth 0), making them easier to detect but impossible for the agent to self-correct.

\subsection{Result 6: Oracle-Guided Repair}
\label{sec:res-causal}

\input{tables/table_oracle_repair}

Oracle-guided repair succeeds on 66\% of failed tasks where ground-truth code is available (\Cref{tab:oracle-repair}). This demonstrates \emph{repairability}, not causal attribution: as the negative control in \Cref{sec:disc-negative-control} shows, injecting the oracle solution produces identical results regardless of which step is replaced, because the ground-truth code independently computes the correct answer. We therefore report oracle repair as an upper bound on reparability rather than as evidence of step-level causal localisation. A full causal analysis with necessity/sufficiency testing is future work.

\subsection{Result 7: Verifier Ablation}
\label{sec:res-verifier}

\input{tables/table_verifier}

\Cref{tab:verifier} reports the McNemar test results. The verifier significantly helps Qwen3-max ($+24.9\%$, $p<0.001$) and GPT-4o ($+19.0\%$, $p<0.001$), but is not significant for GPT-4o-mini, DeepSeek-V3, or DeepSeek-R1. Note that GPT-4o's improvement was not detected by the paired $t$-test in our earlier analysis, highlighting the importance of using the correct statistical test for binary outcomes. The verifier's benefit to Qwen3-max may partly reflect format/protocol fix-up rather than reasoning correction, since Qwen3-max's low baseline performance is partly due to ReAct parser incompatibility.

A key finding is that the verifier's effect is \emph{model-dependent}: it helps models with low-to-moderate baseline performance (Qwen3-max at 18.2\%, GPT-4o at 62.1\%) but not the strongest model (DeepSeek-V3 at 88.4\%) or the weakest (DeepSeek-R1 at 0\%). This suggests that rule-based verification captures a ``middle band'' of errors---those where the model can reason about the problem but benefits from a structured check---but cannot rescue fundamentally broken traces (R1) or improve near-perfect ones (V3).

\begin{figure}[t]
\centering
\includegraphics[width=0.9\linewidth]{figures/verifier_ablation.pdf}
\caption{Verifier ablation. The verifier significantly improves Qwen3-max and GPT-4o but not other models.}
\label{fig:verifier}
\end{figure}

\subsection{Result 8: Token Economics}
\label{sec:res-economics}

\begin{figure}[t]
\centering
\includegraphics[width=0.85\linewidth]{figures/token_economics.pdf}
\caption{Tokens per success vs.\ success rate. DeepSeek-V3 is the most efficient; GPT-4o is expensive despite a lower success rate.}
\label{fig:token}
\end{figure}

\Cref{fig:token} plots tokens-per-success against success rate. DeepSeek-V3 is the most efficient (1{,}943 tokens/success on synthetic), while GPT-4o is the most expensive despite a lower success rate. The cost--accuracy frontier is non-monotonic: the cheapest model (Qwen3-max, 770 tokens/run) has the lowest SR, while the most expensive (GPT-4o, 1{,}241 tokens/run on synthetic but 12{,}792 on DSBench) does not achieve the highest SR. On DSBench, token costs escalate dramatically (10{,}790--12{,}792 tokens/run) because the complex multi-sheet Excel workbooks require longer reasoning traces, yet success rates remain near zero---a cost-effectiveness failure mode unique to real-world data.

\FloatBarrier

\subsection{Result 9: Harness Ablation}
\label{sec:res-harness}

A natural concern is whether silent failures are an artifact of our minimal ReAct harness rather than a genuine model-level phenomenon. To test this, we compare three harnesses on the same 30-task subset of synthetic tasks (6 per domain) with 3 repetitions each:

\begin{itemize}[leftmargin=*,itemsep=1pt]
\item \textbf{H1 (ReAct):} Our baseline minimal ReAct agent (max 8 steps).
\item \textbf{H2 (Single-shot):} One LLM call produces a complete Python script; no multi-step interaction.
\item \textbf{H3 (Few-shot ReAct):} ReAct with one worked example appended to the system prompt.
\end{itemize}

\input{tables/table_harness_ablation}

\Cref{tab:harness-ablation} reports the results. The key finding is that \textbf{silent failures persist across all three harnesses for GPT-4o}: SFR is 100\% under H1 (ReAct), 30\% under H2 (single-shot), and 100\% under H3 (few-shot ReAct). This confirms that silent failures are not an artifact of the minimal ReAct design---they arise even when the agent writes a complete script in a single shot (H2) or receives a worked example (H3). Interestingly, H2 (single-shot) has a \emph{lower} SFR but also a much lower SR (44.4\% vs.\ 86.7\%), because single-shot code more often crashes (producing loud failures) rather than running successfully with a wrong answer. GPT-4o-mini shows silent failures only under H2, suggesting its failures under ReAct manifest as crashes rather than wrong answers. DeepSeek-V3 achieves near-perfect SR under all tested harnesses, producing too few failures for a meaningful SFR comparison. These results validate that our silent-failure findings generalise beyond the minimal ReAct harness.

\FloatBarrier
